# Отчет по аудиту кода Funding Arbitrage Bot

## Дата аудита: 2025-11-13

## Проверенные файлы:
1. `scripts/v2_funding_rate_arb.py` - основная стратегия
2. `hummingbot/connector/derivative/okx_perpetual/okx_perpetual_utils.py` - конфигурация OKX
3. `hummingbot/connector/derivative/hyperliquid_perpetual/hyperliquid_perpetual_utils.py` - конфигурация Hyperliquid
4. `conf/funding_rate_arb.yml` - конфигурация бота

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО:

### 1. Нормализация Funding Rates ✓
**Расположение:** `scripts/v2_funding_rate_arb.py:219-220`

```python
def get_normalized_funding_rate_in_seconds(self, funding_info_report, connector_name):
    return funding_info_report[connector_name].rate / self.funding_payment_interval_map.get(connector_name, 60 * 60 * 8)
```

**Статус:** ✅ КОРРЕКТНО

- OKX: funding каждые 8 часов → делим на 28800 секунд
- Hyperliquid: funding каждый час → делим на 3600 секунд
- Результат: ставка за секунду, которую можно сравнивать между биржами

### 2. Расчет дневной прибыльности ✓
**Расположение:** `scripts/v2_funding_rate_arb.py:212`

```python
funding_rate_diff = abs(rate_connector_1 - rate_connector_2) * self.funding_profitability_interval
```

Где `self.funding_profitability_interval = 60 * 60 * 24` (86400 секунд)

**Статус:** ✅ КОРРЕКТНО

- Берем разницу ставок в секунду
- Умножаем на 86400 (секунд в сутках)
- Получаем дневную прибыльность

### 3. Комиссии бирж ✓
**Расположение:**
- `hummingbot/connector/derivative/okx_perpetual/okx_perpetual_utils.py:10-13`
- `hummingbot/connector/derivative/hyperliquid_perpetual/hyperliquid_perpetual_utils.py:9-13`

**OKX:**
```python
DEFAULT_FEES = TradeFeeSchema(
    maker_percent_fee_decimal=Decimal("0.0002"),  # 0.02%
    taker_percent_fee_decimal=Decimal("0.0005"),  # 0.05%
)
```

**Hyperliquid:**
```python
DEFAULT_FEES = TradeFeeSchema(
    maker_percent_fee_decimal=Decimal("0"),       # 0% (есть rebate -0.02%)
    taker_percent_fee_decimal=Decimal("0.00025"), # 0.025%
)
```

**Статус:** ✅ КОРРЕКТНО - соответствует документации бирж

### 4. Определение стороны сделки ✓
**Расположение:** `scripts/v2_funding_rate_arb.py:214`

```python
trade_side = TradeType.BUY if rate_connector_1 < rate_connector_2 else TradeType.SELL
```

**Статус:** ✅ КОРРЕКТНО

- Если rate_connector_1 < rate_connector_2:
  - LONG на connector_1 (меньшая ставка, платим меньше)
  - SHORT на connector_2 (большая ставка, получаем больше)
- Логика верная!

### 5. Stop Loss логика ✓
**Расположение:** `scripts/v2_funding_rate_arb.py:293-297`

```python
if funding_arbitrage_info["side"] == TradeType.BUY:
    funding_rate_diff = self.get_normalized_funding_rate_in_seconds(funding_info_report, funding_arbitrage_info["connector_2"]) - self.get_normalized_funding_rate_in_seconds(funding_info_report, funding_arbitrage_info["connector_1"])
else:
    funding_rate_diff = self.get_normalized_funding_rate_in_seconds(funding_info_report, funding_arbitrage_info["connector_1"]) - self.get_normalized_funding_rate_in_seconds(funding_info_report, funding_arbitrage_info["connector_2"])
current_funding_condition = funding_rate_diff * self.funding_profitability_interval < self.config.funding_rate_diff_stop_loss
```

**Статус:** ✅ КОРРЕКТНО

- Правильно вычисляет signed разницу ставок (с учетом знака)
- Срабатывает когда спред разворачивается и становится отрицательным

---

## ⚠️ НАЙДЕННЫЕ ПРОБЛЕМЫ:

### ПРОБЛЕМА 1: Некорректный расчет комиссий (MINOR BUG)
**Серьезность:** 🟡 СРЕДНЯЯ (может не влиять на результат, но код некорректен)

**Расположение:** `scripts/v2_funding_rate_arb.py:177-196`

**Проблема:**
```python
estimated_fees_connector_1 = self.connectors[connector_1].get_fee(
    base_currency=trading_pair_1.split("-")[0],
    quote_currency=trading_pair_1.split("-")[1],
    order_type=OrderType.MARKET,
    order_side=TradeType.BUY,  # ❌ ВСЕГДА BUY! (строка 181)
    amount=quote_volume / connector_1_price,
    price=connector_1_price,
    is_maker=False,
    position_action=PositionAction.OPEN
).percent

estimated_fees_connector_2 = self.connectors[connector_2].get_fee(
    base_currency=trading_pair_2.split("-")[0],
    quote_currency=trading_pair_2.split("-")[1],
    order_type=OrderType.MARKET,
    order_side=TradeType.BUY,  # ❌ ВСЕГДА BUY! (строка 191)
    amount=quote_volume / connector_2_price,
    price=connector_2_price,
    is_maker=False,
    position_action=PositionAction.OPEN
).percent
```

**Почему это проблема:**
- Функция получает параметр `side` (TradeType), который указывает направление сделки
- Но при вызове `get_fee()` всегда передается `TradeType.BUY`, игнорируя параметр `side`
- Если `side == TradeType.SELL`, то на connector_1 должен быть SELL, а не BUY

**Влияние:**
- Для OKX и Hyperliquid комиссии одинаковые для BUY и SELL, поэтому результат корректен
- Но если добавить другую биржу с асимметричными комиссиями, это приведет к ошибке

**Исправление:**
```python
# Строка 181 (connector_1):
order_side=side,  # Использовать параметр side

# Строка 191 (connector_2):
order_side=TradeType.BUY if side != TradeType.BUY else TradeType.SELL,  # Противоположная сторона
```

---

### ПРОБЛЕМА 2: Риск деления на ноль (CRITICAL BUG)
**Серьезность:** 🔴 КРИТИЧЕСКАЯ (может вызвать краш бота)

**Расположение:** `scripts/v2_funding_rate_arb.py:364-365`

**Проблема:**
```python
best_paths_info["Days Trade Prof"] = - profitability_after_fees / funding_rate_diff
best_paths_info["Days to TP"] = (self.config.profitability_to_take_profit - profitability_after_fees) / funding_rate_diff
```

**Почему это проблема:**
- Если `funding_rate_diff` равен нулю или очень мал, произойдет:
  - `ZeroDivisionError` - краш программы
  - Или очень большое число (inf), которое сломает отображение
- Это может произойти когда funding rates на обеих биржах одинаковые

**Влияние:**
- Бот может упасть при отображении статуса
- В лучшем случае - некорректное отображение

**Исправление:**
```python
# Добавить проверку на ноль:
if funding_rate_diff > Decimal("0.0001"):  # Минимальный порог
    best_paths_info["Days Trade Prof"] = - profitability_after_fees / funding_rate_diff
    best_paths_info["Days to TP"] = (self.config.profitability_to_take_profit - profitability_after_fees) / funding_rate_diff
else:
    best_paths_info["Days Trade Prof"] = float('inf')
    best_paths_info["Days to TP"] = float('inf')
```

---

### ПРОБЛЕМА 3: Неидеальный хедж позиций (DESIGN ISSUE)
**Серьезность:** 🟡 СРЕДНЯЯ (может привести к небольшому дисбалансу)

**Расположение:** `scripts/v2_funding_rate_arb.py:323, 330, 339`

**Проблема:**
```python
# Строка 323:
position_amount = position_size_quote / price

# Строка 330 (connector_1):
amount=position_amount,

# Строка 339 (connector_2):
amount=position_amount,
```

Используется `price` только от connector_1 для расчета количества. Затем это же количество используется на обеих биржах.

**Почему это проблема:**

Если цены на биржах отличаются:

**Пример:**
- BTC-USD на Hyperliquid: $50,000
- BTC-USDT на OKX: $50,200
- position_size_quote: $1,000
- position_amount = 1000 / 50000 = 0.02 BTC

**Notional values:**
- Connector_1 (Hyperliquid): 0.02 BTC × $50,000 = $1,000 ✓
- Connector_2 (OKX): 0.02 BTC × $50,200 = $1,004 ❌

**Дисбаланс:** $4 (0.4%)

**Влияние:**
- Хедж не идеально сбалансирован
- Возникает небольшой directional risk (риск движения цены)
- Для малых позиций (<$1000) - несущественно
- Для больших позиций (>$10,000) - может быть значимо

**Исправление:**

Нужно вычислять amount отдельно для каждого коннектора:

```python
# Для connector_1:
price_1 = self.market_data_provider.get_price_by_type(
    connector_name=connector_1,
    trading_pair=self.get_trading_pair_for_connector(token, connector_1),
    price_type=PriceType.MidPrice
)
position_amount_1 = position_size_quote / price_1

# Для connector_2:
price_2 = self.market_data_provider.get_price_by_type(
    connector_name=connector_2,
    trading_pair=self.get_trading_pair_for_connector(token, connector_2),
    price_type=PriceType.MidPrice
)
position_amount_2 = position_size_quote / price_2

# Использовать разные amounts
position_executor_config_1 = PositionExecutorConfig(
    ...
    amount=position_amount_1,
    ...
)

position_executor_config_2 = PositionExecutorConfig(
    ...
    amount=position_amount_2,
    ...
)
```

---

### ПРОБЛЕМА 4: Некорректное имя в конфигурации по умолчанию
**Серьезность:** 🟢 НИЗКАЯ (влияет только если используются дефолтные значения)

**Расположение:** `scripts/v2_funding_rate_arb.py:35`

**Проблема:**
```python
connectors: Set[str] = Field(
    default="okx_perpetual,bybit_perpetual,bing_x,hyperliquid_perpetual",
    ...
)
```

**Почему это проблема:**
- `bing_x` - это spot connector, не perpetual
- Если пользователь использует дефолтное значение, бот не сможет торговать на BingX
- Правильное имя: `bingx_perpetual` (если существует)

**Влияние:**
- Минимальное, т.к. в `conf/funding_rate_arb.yml` уже исправлено
- Но может запутать пользователей

**Исправление:**
```python
# Удалить bing_x из дефолта или заменить на bingx_perpetual (если существует)
default="okx_perpetual,bybit_perpetual,hyperliquid_perpetual",
```

---

## 📊 ИТОГОВАЯ ОЦЕНКА:

| Категория | Статус | Комментарий |
|-----------|--------|-------------|
| **Funding Rate нормализация** | ✅ ОТЛИЧНО | Правильно учитывает разные интервалы (1ч vs 8ч) |
| **Расчет прибыльности** | ✅ ХОРОШО | Правильно вычисляет дневную доходность |
| **Комиссии бирж** | ✅ ОТЛИЧНО | Соответствуют официальной документации |
| **Открытие позиций** | ⚠️ ХОРОШО | Логика верна, но есть minor bugs |
| **Закрытие позиций (TP/SL)** | ✅ ОТЛИЧНО | Корректно обрабатывает условия выхода |
| **Хеджирование** | ⚠️ СРЕДНЕ | Не идеально сбалансировано по notional value |
| **Обработка ошибок** | ⚠️ СРЕДНЕ | Нет защиты от деления на ноль |

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ:

### Приоритет 1 (КРИТИЧНО - исправить перед запуском):
1. ✅ **Добавить проверку деления на ноль** (строки 364-365)
   - Может вызвать краш бота

### Приоритет 2 (ВАЖНО - исправить для больших позиций):
2. ✅ **Исправить расчет amounts для идеального хеджа** (строка 323)
   - Критично для позиций > $10,000
   - Менее важно для позиций < $1,000

### Приоритет 3 (ЖЕЛАТЕЛЬНО - улучшение кода):
3. ✅ **Исправить передачу order_side в get_fee()** (строки 181, 191)
   - Не влияет на текущие биржи, но улучшит корректность
4. ✅ **Убрать bing_x из дефолтного значения** (строка 35)
   - Косметическое улучшение

---

## 💰 ВЛИЯНИЕ НА ПРИБЫЛЬНОСТЬ:

### Текущая конфигурация (conf/funding_rate_arb.yml):
```yaml
min_funding_rate_profitability: 0.0015  # 0.15% в день
position_size_quote: 100
leverage: 5
```

**Анализ:**

1. **Минимальный спред 0.15%** - ОПТИМАЛЬНО ✅
   - С учетом комиссий taker: 0.075% (вход) + 0.075% (выход) = 0.15%
   - Это безубыточная точка для market orders
   - С maker orders: комиссии ~0%, все 0.15% - чистая прибыль

2. **Position size $100** - БЕЗОПАСНО ✅
   - При leverage 5x контролируем $500
   - Liquidation только при >20% движении цены
   - Рекомендуемо для начинающих

3. **Leverage 5x** - УМЕРЕННЫЙ РИСК ⚠️
   - Допустимо для опытных трейдеров
   - Для начинающих лучше 3x
   - Риск ликвидации при ~20% движении

**Ожидаемая доходность (при спреде 0.15%):**
- Дневная: 0.15% × $100 = $0.15
- Месячная: $0.15 × 30 = $4.50 (4.5% в месяц)
- Годовая: 54.75% APR

**При спреде 0.5% (оптимистичный сценарий):**
- Дневная: 0.5% × $100 = $0.50
- Месячная: $15 (15% в месяц)
- Годовая: 182.5% APR

---

## 🎯 ВЫВОДЫ:

### ЧТО РАБОТАЕТ ХОРОШО:
1. ✅ Основная логика funding arbitrage правильная
2. ✅ Нормализация ставок между биржами корректна
3. ✅ Комиссии соответствуют документации
4. ✅ Stop loss и take profit работают правильно
5. ✅ Конфигурация оптимальна для баланса риска и доходности

### ЧТО НУЖНО ИСПРАВИТЬ:
1. 🔴 **Критично:** Добавить защиту от деления на ноль
2. 🟡 **Важно:** Улучшить хеджирование для больших позиций
3. 🟢 **Желательно:** Мелкие улучшения кода

### МОЖНО ЛИ ЗАПУСКАТЬ?
**Ответ:** ✅ **ДА, но с ограничениями**

**Условия безопасного запуска:**
- ✅ Размер позиции ≤ $500 (дисбаланс хеджа минимален)
- ✅ Leverage ≤ 5x (защита от ликвидации)
- ✅ Мониторинг 2-3 раза в день
- ⚠️ Следить за логами на наличие ошибок деления на ноль

**Рекомендуется исправить ПЕРЕД запуском:**
- 🔴 Проблему #2 (деление на ноль) - может вызвать краш

**Можно исправить ПОСЛЕ первого теста:**
- 🟡 Проблему #3 (хеджирование) - при увеличении размера позиций
- 🟢 Проблемы #1, #4 (косметические улучшения)

---

## 📝 СЛЕДУЮЩИЕ ШАГИ:

1. **Исправить критические баги** (Проблема #2)
2. **Протестировать на маленьких суммах** ($50-100)
3. **Мониторить первые 24-48 часов**
4. **Постепенно увеличивать размер позиций**
5. **Оптимизировать хеджирование** (Проблема #3)

---

**Дата отчета:** 2025-11-13
**Аудитор:** Claude Code Assistant
**Версия бота:** v2_funding_rate_arb
